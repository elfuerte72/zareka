# Локальный инструментарий Claude Code / скиллы — не часть Зареки
.claude/
.agents/
skills-lock.json

# Аудио гимна не распространяем в публичном репозитории (права на запись).
# Пользователь кладёт свой файл локально — см. README.
*.mp3

.DS_Store
node_modules/

# Референсные изображения не распространяем (права)
*.jpeg
*.jpg
*.png
